# Spanel Bun 开发容器
# 项目专用容器 - Playwright MCP 请使用通用容器

FROM docker.io/oven/bun:latest

# 设置工作目录
WORKDIR /app

# 安装基础开发工具
RUN apt-get update && apt-get install -y \
    git \
    curl \
    wget \
    python3 \
    python3-pip \
    build-essential \
    && rm -rf /var/lib/apt/lists/*

# 设置环境变量
ENV NODE_ENV=development

# 默认命令
CMD ["tail", "-f", "/dev/null"]
