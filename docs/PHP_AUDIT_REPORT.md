# SPanel PHP 项目审计报告

## 📋 执行概要

**审计日期**: 2026-01-13
**旧项目位置**: `/root/git/spanel-bun/.links/spanel`
**新项目位置**: `/root/git/spanel-bun`
**审计目标**: 识别核心 PHP 逻辑，规划向 Bun + TypeScript 的迁移路径

---

## 🏗️ 旧项目架构分析

### 技术栈
- **框架**: Slim 3.x (PHP 微框架)
- **模板引擎**: Smarty 3.x
- **数据库**: MySQL/MariaDB
- **ORM**: Illuminate Database (Laravel 组件)
- **缓存**: Redis (Predis 客户端)
- **前端**: Vue.js + jQuery

### 项目结构
```
spanel/
├── app/
│   ├── Controllers/       # 控制器层 (核心业务逻辑)
│   │   ├── AuthController.php        # 用户认证
│   │   ├── UserController.php        # 用户管理
│   │   ├── AdminController.php       # 管理后台
│   │   ├── ApiController.php         # RESTful API
│   │   ├── LinkController.php        # 订阅链接生成
│   │   ├── Admin/                    # 管理员子控制器
│   │   ├── Client/                   # 客户端 API
│   │   ├── Mu/                       # Mu 协议支持
│   │   └── Mod_Mu/                   # Mod_Mu 协议
│   ├── Models/            # 数据模型层
│   │   ├── User.php                 # 用户模型
│   │   ├── Node.php                 # 节点模型
│   │   ├── TrafficLog.php           # 流量日志
│   │   ├── Code.php                 # 充值码
│   │   ├── Coupon.php               # 优惠券
│   │   ├── InviteCode.php           # 邀请码
│   │   ├── Bought.php               # 购买记录
│   │   ├── Payback.php              # 返利记录
│   │   ├── Ticket.php               # 工单
│   │   └── Shop.php                 # 商品
│   ├── Services/          # 业务服务层
│   │   ├── Auth/                    # 认证服务
│   │   ├── Mail/                    # 邮件服务
│   │   ├── Gateway/                 # 支付网关
│   │   ├── Config.php               # 配置管理
│   │   └── Analytics.php            # 数据分析
│   ├── Middleware/        # 中间件
│   │   ├── Auth.php                 # 认证中间件
│   │   ├── Admin.php                # 管理员中间件
│   │   └── Api.php                  # API 中间件
│   └── Utils/             # 工具类
│       ├── Tools.php                # 通用工具
│       ├── Hash.php                 # 加密工具
│       ├── Helper.php               # 辅助函数
│       ├── Geetest.php              # 验证码
│       └── Telegram.php             # Telegram Bot
├── config/                # 配置文件
│   ├── .config.php               # 主配置
│   └── routes.php                # 路由配置
├── public/                # 公共资源
│   ├── theme/                    # 前端主题
│   └── vuedist/                  # Vue 构建
├── sql/                   # 数据库文件
└── storage/               # 存储目录
```

---

## 🔍 核心模块识别

### 1. 用户认证模块 (Authentication)

**关键文件**:
- `app/Controllers/AuthController.php` - 登录/注册/密码重置
- `app/Services/Auth/` - 认证服务
- `app/Models/User.php` - 用户数据模型
- `app/Middleware/Auth.php` - 认证中间件

**核心功能**:
```php
// 登录逻辑
- email/passwd 验证
- 验证码支持 (Geetest/ReCaptcha)
- Telegram Bot 集成
- 记住登录状态
- 登录 IP 记录

// 注册逻辑
- 邀请码验证
- 邮箱验证
- 初始流量分配
- 用户等级设置
- 随机端口分配

// 密码管理
- 密码重置
- 邮件验证链接
- 密码加密 (SHA256/Argon2i)
```

**数据流**:
```
用户输入 → 验证码验证 → 邀请码检查 → 密码加密 → 用户创建 → 初始化配置 → 发送欢迎邮件
```

---

### 2. 节点管理模块 (Node Management)

**关键文件**:
- `app/Models/Node.php` - 节点模型
- `app/Controllers/Admin/NodeController.php` - 节点管理
- `app/Controllers/ApiController.php` - 节点 API

**核心功能**:
```php
// 节点类型
- Shadowsocks (SS)
- ShadowsocksR (SSR)
- V2Ray/VLESS
- Trojan
- Mu 协议 (单端口多用户)

// 节点状态监控
- 心跳检测 (node_heartbeat)
- 在线用户数
- 负载监控
- GFW 检测
- 节点分组和等级限制
```

**数据结构**:
```sql
CREATE TABLE node (
  id INT PRIMARY KEY,
  name VARCHAR(255),              -- 节点名称
  server VARCHAR(255),            -- 服务器地址
  type VARCHAR(32),               -- 节点类型
  method VARCHAR(32),             -- 加密方式
  node_group INT,                 -- 节点分组
  node_class INT,                 -- 节点等级要求
  status VARCHAR(10),             -- 节点状态
  node_heartbeat INT,             -- 最后心跳时间
  traffic_rate VARCHAR(32),       -- 流量倍率
  node_speedlimit VARCHAR(32),    -- 速度限制
  region VARCHAR(255)             -- 节点地区
);
```

---

### 3. 订阅服务模块 (Subscription Service)

**关键文件**:
- `app/Controllers/LinkController.php` - 订阅链接生成
- `app/Controllers/Mu/UserController.php` - Mu 协议支持
- `app/Controllers/Mod_Mu/UserController.php` - Mod_Mu 支持

**核心功能**:
```php
// 订阅链接生成
- SS/SSR 订阅
- V2Ray 订阅
- Trojan 订阅
- Clash 订阅
- Quantumult 订阅
- Surfboard 订阅

// 配置生成逻辑
- 节点过滤 (等级/分组)
- 流量统计
- 在线设备管理
- 订阅转换
- 节点名混淆
```

**URL 格式**:
```
https://test-spanel.freessr.bid/link/{token}?mu=0
```

---

### 4. 流量统计模块 (Traffic Analytics)

**关键文件**:
- `app/Models/TrafficLog.php` - 流量日志
- `app/Models/AliveIp.php` - 活跃 IP
- `app/Models/NodeOnlineLog.php` - 节点在线日志
- `app/Services/Analytics.php` - 分析服务

**核心功能**:
```php
// 流量记录
- 上传/下载流量 (u/d)
- 节点流量统计
- 用户流量限制
- 流量重置规则
- 流量倍率计算

// 在线监控
- 活跃 IP 记录
- 在线用户数
- 连接时间统计
- 多设备检测
```

**数据结构**:
```sql
CREATE TABLE user_traffic_log (
  id BIGINT PRIMARY KEY,
  user_id INT,
  node_id INT,
  traffic BIGINT,                -- 流量大小 (字节)
  log_time INT                   -- 记录时间戳
);

CREATE TABLE alive_ip (
  id BIGINT PRIMARY KEY,
  nodeid INT,
  userid INT,
  ip TEXT,                       -- IP 地址
  datetime BIGINT                -- 记录时间戳
);
```

---

### 5. 支付系统模块 (Payment Gateway)

**关键文件**:
- `app/Services/Gateway/` - 支付网关
- `app/Controllers/QuickPayFunction.php` - 快速支付
- `app/Models/Bought.php` - 购买记录
- `app/Models/Code.php` - 充值码
- `app/Models/Coupon.php` - 优惠券

**核心功能**:
```php
// 支付方式
- 支付宝 (F2FPay/SPay)
- 微信支付
- PayPal
- PaymentWall
- 码支付 (CodePay)
- ClonePay
- 充值码支付

// 商品系统
- 套餐购买
- 流量包
- 过期时间延长
- 优惠券折扣
- 返利系统
```

**支付流程**:
```
选择商品 → 应用优惠券 → 选择支付方式 → 创建订单 → 支付回调 → 到账处理 → 发送通知
```

---

### 6. 工单系统模块 (Ticket System)

**关键文件**:
- `app/Controllers/Admin/TicketController.php` - 工单管理
- `app/Models/Ticket.php` - 工单模型

**核心功能**:
```php
// 工单管理
- 工单提交
- 工单回复
- 工单关闭
- 状态通知
- 提交费用 (ticket_price)
```

---

### 7. 邮件服务模块 (Mail Service)

**关键文件**:
- `app/Services/Mail/` - 邮件服务
- `app/Services/Mail.php` - 邮件管理器

**核心功能**:
```php
// 邮件类型
- 注册验证邮件
- 密码重置邮件
- 工单通知邮件
- 系统公告邮件
- 流量提醒邮件

// 邮件驱动
- SMTP
- Mailgun
- SendGrid
```

---

### 8. Telegram Bot 模块

**关键文件**:
- `app/Utils/Telegram.php` - Telegram 工具
- `app/Utils/TelegramSessionManager.php` - 会话管理

**核心功能**:
```php
// Bot 功能
- 用户绑定
- 流量查询
- 节点状态
- 工单提交
- 签到提醒
- 图灵机器人对话
```

---

## 📊 数据库结构

### 核心表

```sql
-- 用户表
user
├── id, email, username, pass
├── port (分配端口)
├── transfer_enable (总流量)
├── u, d (上传/下载流量)
├── class (用户等级)
├── expire_in (账户到期时间)
├── node_group (节点分组权限)
├── money (账户余额)
├── ref_by, ref_by_count (邀请关系)
└── is_admin (管理员标识)

-- 节点表
node
├── id, name, server
├── type (节点类型: SS/SSR/V2Ray/Trojan)
├── method (加密方式)
├── node_group, node_class
├── status (节点状态)
├── node_heartbeat (心跳时间)
├── traffic_rate (流量倍率)
└── region (节点地区)

-- 流量日志表
user_traffic_log
├── id, user_id, node_id
├── traffic (流量)
└── log_time (记录时间)

-- 在线IP表
alive_ip
├── id, nodeid, userid
├── ip (IP地址)
└── datetime (记录时间)

-- 充值码表
code
├── id, code, type
├── number (金额)
├── isused (是否使用)
├── userid (使用者)
└── usedatetime (使用时间)

-- 优惠券表
coupon
├── id, code
├── credit (面值)
├── use_limit, used_count
└── expire (到期时间)

-- 购买记录表
bought
├── id, userid
├── shop (购买内容 JSON)
├── datetime (购买时间)
├── price (价格)
└── coupon (使用的优惠券)

-- 返利记录表
payback
├── id, userid (获得返利用户)
├── ref_userid (返利用户)
├── ref_get (返利金额)
└── datetime (返利时间)

-- 邀请码表
invite_code
├── id, code
├── user_id (生成者)
├── used_userid (使用者)
├── created_at, used_at
└── status (状态)

-- 工单表
ticket
├── id, userid
├── title, content
├── status (状态: 0开放 1已回复 2已关闭)
├── reply (回复内容)
└── datetime (创建时间)

-- 系统配置表
config
├── id, item (配置项)
├── value (配置值)
└── class (配置类型)

-- 公告表
announcement
├── id, date
└── content (公告内容 HTML)

-- 订阅链接表
link
├── id, userid
├── type (链接类型)
├── address, port, token
├── ios (iOS 支持)
└── method (加密方式)
```

---

## 🔐 安全机制

### 1. 认证安全
- 密码加密: SHA256 / Argon2i
- JWT Token 认证
- Session 管理 (Cookie/Redis)
- 登录 IP 记录和验证
- 异地登录提醒

### 2. 验证码
- Geetest 极验
- Google ReCaptcha
- 邮箱验证码

### 3. 访问控制
- 用户等级限制 (user.class)
- 节点分组权限 (node_group)
- 管理员权限 (is_admin)
- IP 黑白名单

### 4. 防护机制
- AntiXSS (XSS 防护)
- SQL 注入防护 (ORM)
- CSRF Token
- 频率限制

---

## 🎯 关键发现与建议

### 优点
1. ✅ **清晰的 MVC 架构**: 控制器、模型、服务层分离明确
2. ✅ **丰富的功能模块**: 用户管理、节点管理、支付系统完整
3. ✅ **灵活的配置系统**: 基于数据库的动态配置
4. ✅ **完善的监控机制**: 节点心跳、在线用户、流量统计
5. ✅ **多协议支持**: SS/SSR/V2Ray/Trojan 全覆盖

### 需要改进的地方
1. ⚠️ **代码重复**: 部分逻辑在多个控制器中重复
2. ⚠️ **缺少单元测试**: 未发现完整的测试覆盖
3. ⚠️ **错误处理不统一**: 部分地方直接返回错误
4. ⚠️ **文档不足**: 缺少 API 文档和开发文档
5. ⚠️ **性能优化空间**: 可以增加更多缓存策略

---

## 📦 迁移建议

### 优先级划分

#### P0 - 核心基础 (必须优先完成)
- [ ] 用户认证系统
- [ ] 节点管理基础
- [ ] 流量统计
- [ ] 基础 API 框架

#### P1 - 核心功能 (第二阶段)
- [ ] 订阅链接生成
- [ ] 支付系统集成
- [ ] 工单系统
- [ ] 邮件服务

#### P2 - 增强功能 (第三阶段)
- [ ] Telegram Bot
- [ ] 管理后台
- [ ] 数据分析和报表
- [ ] 高级监控功能

---

## 🔗 相关文档

- [旧项目 README](.links/spanel/README.md)
- [旧项目配置](.links/spanel/config/.config.php)
- [数据结构文档](.links/spanel/README.md#数据结构)
- [新项目 README](README.md)
- [迁移计划](MIGRATION_PLAN.md) (待创建)

---

**报告生成时间**: 2026-01-13
**审计人员**: Claude (AI Assistant)
**版本**: v1.0.0
